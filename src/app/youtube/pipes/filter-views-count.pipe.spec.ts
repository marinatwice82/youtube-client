import { FilterViewsCountPipe } from './filter-views-count.pipe';

describe('FilterViewsCountPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterViewsCountPipe();
    expect(pipe).toBeTruthy();
  });
});
